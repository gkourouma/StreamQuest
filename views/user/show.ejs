<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %></title>
</head>
<body>
    <h1><%= movie.title %></h1>
    <img src="<%= movie.poster %>" alt="<%= movie.title %>">
    <h3><%= movie.overview %></h3>
    <p><%= movie.genre %></p>
    <%= movie.releaseYear %>
    <p>Rating: <%= movie.rating %> out of 10</p>
    <%= movie.runTime %>

  <form action="/user/watchlist/<%= movie._id %>/add" method="POST">
    <button type="submit">Add to Watchlist</button>
  </form>

  <a href="/user/homepage">Back to Home</a>
  <a href="/user/watchlist">Go to Watchlist</a>
</body>
</html>